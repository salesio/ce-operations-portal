import { apiClient } from "./apiClient"; import type { EntityId } from "../../types/entities"; type P=Record<string,unknown>;
export const listNotifications=()=>apiClient.get<P[]>("/notifications"); export const getNotificationById=(id:EntityId)=>apiClient.get<P>(`/notifications/${id}`); export const createNotification=(p:P)=>apiClient.post<P>("/notifications",p); export const updateNotification=(id:EntityId,p:P)=>apiClient.patch<P>(`/notifications/${id}`,p); export const listNotificationTemplates=()=>apiClient.get<P[]>("/notifications/templates"); export const listNotificationPreferences=()=>apiClient.get<P[]>("/notifications/preferences");
