import type { FoundationTeacher } from "../types/entities";

/**
 * Foundation School Teachers Seed.
 * Cleared of mock/demo teachers so the dashboard reads and writes directly
 * to Supabase (public.foundation_school_teachers).
 */
export const FOUNDATION_TEACHERS_SEED: FoundationTeacher[] = [];

